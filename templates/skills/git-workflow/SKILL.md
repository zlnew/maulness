---
name: git-workflow
description: Standard git branching, clean diff inspection, and Maul final sign-off rules
---
# Git Workflow

1. Always verify working tree with `git status` before and after operations.
2. Produce small, focused, self-contained local commits.
3. Maul retains final sign-off — NEVER push to remote or open PRs without explicit approval.
